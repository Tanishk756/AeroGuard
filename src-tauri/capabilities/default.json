{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability configuration for the AeroGuard desktop application",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
